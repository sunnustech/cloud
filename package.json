{
  "name": "functions",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "setup": "yarn && yarn --cwd ./functions && yarn --cwd ./tests",
    "serve": "yarn --cwd ./functions serve"
  }
}
